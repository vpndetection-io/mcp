{
    "name": "vpndetection-mcp-integration",
    "version": "0.0.0",
    "private": true,
    "description": "Exercises the PUBLISHED vpndetection-mcp package against the staging API.",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.20.2",
        "vpndetection-mcp": "^4.0.0"
    },
    "scripts": {
        "test": "node --test test/*.test.mjs",
        "integration": "node scripts/run.mjs"
    },
    "devDependencies": {
        "@mslm/libjs-eslint-config": "2.4.0"
    }
}
